-- MoltLoop seed data
-- This file runs after all migrations

-- Note: Admin users must first exist in auth.users (via Supabase Auth).
-- After creating an admin user through Supabase Auth, insert them here:
--
-- INSERT INTO admins (user_id, email, role)
-- VALUES ('your-auth-user-uuid', 'admin@moltloop.com', 'super_admin');

SELECT 'MoltLoop seed data loaded' AS status;
